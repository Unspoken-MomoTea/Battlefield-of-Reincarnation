name: World Engine P1 Plan Consistency Review

on:
  workflow_dispatch:

jobs:
  completed:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'P1 plan review already completed; temporary workflow will be removed before merge.'
