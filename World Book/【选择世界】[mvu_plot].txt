<%_
;(() => {
  /* 如果不是单一世界模式，才输出穿越协议 */
  const stat_data = getMessageVar('stat_data', { defaults: {} }) || {};
  const isSingleWorld = _.get(stat_data, '设置.单一世界', false) === true;
  if (!isSingleWorld) { 
_%>
<System_Rules>
触发条件:
  - 当且仅当输入中明确包含【选择世界】时触发
  - 前置条件：主角必须处于主神空间或休整期。未满足则拒绝并继续当前叙事
  - 候选纪律：选单阶段【绝对禁止】输出 `<UpdateVariable>` 修改任何后台数据，此时仅作前瞻展示

世界协议根基:
  - 优先原则: 世界是真实运行的平行宇宙，绝不存在“等待玩家触发”的冻结副本。任务必须服从世界法则
  - 最小干涉: 主神仅负责【锚定目标】、【投放】与【结算】，绝不直接下场修改世界物理法则
  - 阵营导向: 任务目标必须根据玩家当前的【阵营】(守护者/篡夺者/织梦者/残魂) 进行深度定制，互斥且独立

世界生成原则:
  - 优先真实IP: 优先使用真实存在的成熟作品(动漫/游戏/电影/小说)作为任务世界。若玩家仅指定题材，优先匹配该题材公认代表性的成熟IP；若存在多个代表IP，可随机选择其中之一
  - 严禁换皮: 禁止对已有成熟IP进行改名、换皮、删改专有名词或伪原创
  - 禁止原创: 8个候选世界必须来自真实存在的成熟作品，禁止编造、换皮或使用不存在的IP
  - 真实IP匹配锁:
    - IP必须符合【主模块】，并能合理承载【副模块】
    - 副模块可通过原著设定、时间锚点、地点或当前剧情成立
    - 无法合理承载副模块时必须更换IP，禁止修改主模块或副模块
    - 禁止为落实副模块编造与原著冲突的底层设定
  原著一致性校验:
    - 完成每个世界生成后，AI必须逐项检查：
        - 世界名称
        - 人物
        - 势力
        - 地点
        - 时间锚点
        - 主线状态
        - 世界法则
        - 切入身份
    - 上述内容均必须基于同一原著IP
    - 严禁混入任何其他作品的人物、组织、地点、能力体系、剧情事件或世界设定
    - 严禁借用其他IP的剧情结构、组织定位、世界格局或核心设定，即使名称不同也视为违规
    - 任意一项不一致，必须整条世界重新生成

<%_
  // ============================================================
  // 预生成模块：主模块池 & 副模块池
  // ============================================================
  const mainPool = [
    '低武江湖', '高武大荒', '古典修仙', '洪荒神话', '东方志异',
    '中式民俗禁忌', '日式怪谈异闻', '诡异民俗', '不明生物', '都市传说',

    '真实历史战役', '帝国征服史诗', '二战风云', '冷战谍影', '阵营对抗',
    '架空历史战争', '钢铁阵营决战', '星际全面战争', '史诗族群阵线',

    '日常都市', '都市异能', '黑帮谍战', '智斗博弈', '现代怪异', '超凡竞技',
    '灵异侦探', '都市危机', '凶案现场',
    '硬核科幻', '太空歌剧', '赛博朋克', '废土生存', '机甲巨兽',
    '末日生化', '智械危机', '星际虫灾', '深空幽寂',
    '低魔中世纪', '高魔史诗', '蒸汽维多利亚', '暗黑魂系', 
    '暗黑哥特', '魔法学院', '瘟疫诡镇', '旧宅怨灵',
    '克苏鲁神话', '异常收容', '规则怪谈', '梦核超现实', 
    '童话反转', '阈限空间', '日式轻小说', '架空异界',
    '不可视存在', '精神污染区', '末日预兆',
    '开放世界', '游戏世界', '虚拟网游', '死后世界', '历史演义', '美漫超英', '格斗擂台', '硬核动作',
    '深渊地狱', '热血王道', '荒诞喜剧',
    '天体陨落', '死亡预演', '巨兽觉醒', '神话传说',
    'Galgame向', '黄游里番', 'ABO世界', '魔物娘', '赛博极乐城', '魔法少女',
  ];
  const subPool = [
    '大逃杀', '绝境求生', '天灾降临', '死亡倒计时', '世界污染',

    '冷兵器限定', '近战优势', '远程优势', '无限制格斗', '黑暗森林法则',

    '阵营对抗', '猎物与猎手', '悬赏通缉', '零和博弈', '非对称对抗', 
    '据点争夺', '两军对垒', '斩首行动', '护送任务', '物资垄断',

    '全员禁魔', '科技锁死', '属性压制', '禁止杀戮', '禁止治疗',
    '表里世界', '原著附身', '身份互换', '伤痛共享', '时间狂化',

    '地牢深潜', '遗迹破译', '无尽怪海', '首领狂暴', '百鬼夜行', 
    '巨物围猎', '寻宝行动', '死亡机关', '深渊逃脱', '高位注视',

    '领地塔防', '攻城掠地', '战争兵器', '信仰争夺', '世界Boss',

    '强制发情期', '体液补给', '败者奴役', '魅惑判定', '性转世界', '末日繁衍', '淫乱常识洗脑'
  ];

  // 辅助：从数组中抽取不重复的 n 个元素（Fisher–Yates）
  function pickRandomUnique(arr, n) {
    const copy = [...arr];
    for (let i = copy.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [copy[i], copy[j]] = [copy[j], copy[i]];
    }
    return copy.slice(0, n);
  }

  // 抽取 8 个不重复的主模块
  const mainModules = pickRandomUnique(mainPool, 8);
  // 为每个世界独立随机分配一个副模块（允许重复）
  const subModules = Array.from({ length: 8 }, () => subPool[Math.floor(Math.random() * subPool.length)]);
  // 按 15:10:10:5 权重为每个世界锁定IP热度约束，仅供匹配且不得输出
  const ipHeatPool = [
    ...Array(15).fill('较冷门'), ...Array(10).fill('中等'),
    ...Array(10).fill('较热门'), ...Array(5).fill('世界知名')
  ];
  const ipHeatTypes = Array.from({ length: 8 }, () => ipHeatPool[Math.floor(Math.random() * ipHeatPool.length)]);

  // 生态分布：3无异端 + 3死斗局 + 2混沌局，然后打乱顺序
  const ecologyBase = ['无异端', '无异端', '无异端', '死斗局', '死斗局', '死斗局', '混沌局', '混沌局'];
  for (let i = ecologyBase.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [ecologyBase[i], ecologyBase[j]] = [ecologyBase[j], ecologyBase[i]];
  }
  const ecologyTypes = ecologyBase;

  // 难度锁定：以玩家当前层级为基准，一次性为8个候选分配并计算最终区间
  const difficultyTiers = ['F', 'E', 'D', 'C', 'B', 'A', 'S', 'SS', 'SSS'];
  const playerTierIndex = ['Ⅰ', 'Ⅱ', 'Ⅲ', 'Ⅳ', 'Ⅴ', 'Ⅵ', 'Ⅶ', 'Ⅷ', 'Ⅸ']
    .indexOf(String(_.get(stat_data, '主角.层级', 'Ⅰ')).trim());
  const baseDifficultyIndex = playerTierIndex >= 0 ? playerTierIndex : 0;
  const identityText = [].concat(_.get(stat_data, '主角.身份', []) || []).join('|');
  const isRemnant = identityText.includes('残魂');
  const shuffle = (arr) => {
    const copy = [...arr];
    for (let i = copy.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [copy[i], copy[j]] = [copy[j], copy[i]];
    }
    return copy;
  };
  const difficultyOffsets = Array(8).fill(0);
  if (isRemnant) {
    const deathmatchIndexes = ecologyTypes
      .map((ecology, idx) => ecology === '死斗局' ? idx : -1)
      .filter(idx => idx >= 0);
    shuffle([2, 1, 1]).forEach((offset, idx) => {
      difficultyOffsets[deathmatchIndexes[idx]] = offset;
    });
    const otherIndexes = shuffle(
      ecologyTypes.map((_, idx) => idx).filter(idx => !deathmatchIndexes.includes(idx))
    );
    shuffle([0, 0, 0, 0, -1]).forEach((offset, idx) => {
      difficultyOffsets[otherIndexes[idx]] = offset;
    });
  } else {
    shuffle([0, 0, 0, 0, 1, 1, 2, -1]).forEach((offset, idx) => {
      difficultyOffsets[idx] = offset;
    });
  }

  const difficultyRanges = ecologyTypes.map((ecology, idx) => {
    const lowerIndex = Math.max(0, Math.min(8, baseDifficultyIndex + difficultyOffsets[idx]));
    const upperStep = ecology === '无异端' ? 1 : 2;
    const upperIndex = Math.min(8, lowerIndex + upperStep);
    return `${difficultyTiers[lowerIndex]}~${difficultyTiers[upperIndex]}`;
  });

  // 构建世界数据数组
  const worldData = mainModules.map((main, idx) => ({
    main,
    sub: subModules[idx],
    ecology: ecologyTypes[idx],
    ipHeat: ipHeatTypes[idx],
    difficulty: difficultyRanges[idx],
  }));
_%>

以下为系统随机抽取的 8 个世界原始数据，请严格依据此列表生成后续 <mission> 中的每个世界条目：
<%_ worldData.forEach((w, i) => { _%>
<%- i+1 %>. 主模块: 【<%- w.main %>】, 副模块: 【<%- w.sub %>】, 生态: 【<%- w.ecology %>】, 难度: 【<%- w.difficulty %>】, IP热度: 【<%- w.ipHeat %>】
<%_ }); _%>

多样性引擎与匹配生态:
  核心定义:
    - 本模块不再负责任何随机
    - 本模块不再重新抽取世界类型
    - 本模块不再重新抽取世界机制
    - 本模块不再重新分配生态
    - 本模块不再重新判定IP热度
    - 本模块不再重新分配难度层级
    - 本模块仅负责读取上方已经生成的固定结果，并将其渲染为8个具体世界
  随机源唯一性:
    - 本次【选择世界】的随机源只有上方生成的世界数据
    - AI不得在任何生成阶段自行调用随机逻辑
    - AI不得以“随机”“适配”“更合理”“更符合剧情”等理由修改已生成结果
    - AI不得重新生成新的主模块、副模块、生态、IP热度或难度
  生成映射:
    - 第1个世界只能使用上方第1条随机数据
    - 第2个世界只能使用上方第2条随机数据
    ...以此类推
  1. 【主模块映射】:
    - 根据对应结果中的【主模块】，匹配符合该主模块的真实成熟IP
    - 主模块是强制世界类型约束，不是关键词提示
    - AI不得因为副模块、生态或自身偏好更换主模块
    - AI不得选择与主模块核心世界观明显不符的IP
  2. 【副模块映射】:
    - 副模块是强制玩法，必须同时体现在【干涉模式】与【主线状态】
    - 实现必须符合当前IP的世界观与剧情
    - 禁止根据玩家技能、装备、血统、形态定制副模块实现
    - 副模块的核心效果直接成立，不需要世界法则为其提供理由；禁止将副模块换名后写入世界法则
  3. 【生态映射】:
    - 根据对应结果中的【生态】，严格决定该世界的异端结构
    - 【无异端】:
      - 预计异端必须为0人。
      - 不得出现其他轮回者、契约者、异端守护者或同类外来干涉者
      - 冲突对象只能来自本土势力、原著角色、世界环境或世界本身
    - 【死斗局】:
      - 必须存在其他轮回者或异端
      - 必须存在明确的敌对关系
      - 敌对目标之间存在不可调和的核心利益冲突
    - 【混沌局】:
      - 必须存在多方轮回者、异端或外来势力
      - 不得只存在单一敌对势力
      - 必须形成多方利益交锋、临时结盟、互相利用或背叛关系
  4. 【IP热度】:
    - IP热度只用于筛选符合主模块的具体作品，不得写入最终 `<mission>`
    - 必须选择符合对应热度范围的真实IP；禁止虚构作品或伪造冷热门程度
    - 若IP与主模块或热度不符，必须更换IP，不得修改随机结果
  5. 【五项绑定不可改变】:
    - 主模块 = 对应结果中的主模块
    - 副模块 = 对应结果中的副模块
    - 生态 = 对应结果中的生态
    - 副本难度 = 对应结果中的难度
    - IP热度 = 对应结果中的IP热度约束
    - 五者任何一项不一致，均视为该世界生成错误
    - 生成错误时，必须按照对应结果重写当前世界
  6. 生态与副模块冲突处理:
    - 【生态】决定外来干涉者之间的关系结构
    - 【副模块】决定世界中的特殊机制或局势机制
    - 二者可以同时存在。
    - “禁止杀戮 + 死斗局”并不等于删除死斗局
    - 此时死斗局可以表现为非直接杀戮的敌对竞争，例如:
      - 囚禁
      - 夺取目标
      - 资源封锁
      - 阵营淘汰
      - 强制驱逐
      - 任务失败淘汰
    - AI不得因为副模块与生态存在表面冲突而删除、替换或重新随机其中任何一项

世界位格:
  - 根据当前时间锚点与主线状态决定副本世界允许存在的生命层级上限
  - 不受玩家当前评级影响
  - 依据世界核心体系与世界允许的最高生命层级判定
  - 严格遵守《生命层级与社会生态》设定

副本难度锁定协议:
  - 每个候选的初始难度已经由上方EJS完成分配与计算，并封顶SSS
  - AI必须逐项原样复制对应预生成数据中的【难度】，禁止重新计算、调换或升降
  - 禁止依据IP强弱、时间锚点、主线状态、终局危机或世界法则异常修改候选难度
  - 最终格式固定为：[副本难度]: ${对应预生成数据中的难度}级；即使上下限同级也必须保留“~”
  - 世界位格 = 世界允许存在的生命层级上限
  - 副本难度 = 玩家本次遭遇的生存压力
  - 两者完全独立，世界位格不得参与副本难度计算

世界法则输出锁:
  - 遵守<世界因果与法则协议>，生成0~10条；可为0，禁止凑数
  - 只输出形成明确【能/不能、有效/无效、可/不可影响】边界的规则
  - 禁止环境描述、风险提示、战术优劣及任何程度型软修正
  - 禁止根据玩家Build定制
  - 副模块不得改名、解释或重复包装成世界法则
  - 输出前删除所有不合格法则，禁止为了数量补充

输出格式:
  规则: 全局仅在最外层使用一对 `<mission>...</mission>` 标签包裹！内部使用 `1. 2. 3.` 序号
  必须严格按照下方模板，并**严格依照上方预生成列表的顺序**生成 8 个世界条目，不得增减或调换

template: |
  <mission>
  1. 【《${真实原著IP名称}》】 (世界位格: ${Ⅰ~Ⅸ})
    [主模块]: ${对应预生成数据中的主模块}
    [副模块]: ${对应预生成数据中的副模块}
    [生态]: ${对应预生成数据中的生态}
    [副本难度]: ${对应预生成数据中的难度}级
    [时间锚点]: ${简述剧情时间锚点，需与主模块匹配}
    [干涉模式]: ${结合玩家阵营、主模块与副模块，描述在该机制下与本土势力或异端的具体博弈/冲突态势} (预计异端: ${0~9}人)
    [主线状态]: ${当前剧情时间锚点内容，需体现副模块带来的局势变化}
    [世界法则]: ${严格按【世界法则输出锁】写0~10条，以分号分隔}
    [${切入/夺舍}身份]: ${系统合法切入或残魂夺舍的初始身份}
  2. 【《${真实原著IP名称}》】 (世界位格: ${Ⅰ~Ⅸ})
      ...
  (严格按此紧凑格式，依次输出至第8个世界)
  </mission>

</System_Rules>
<%_
  }
})();
_%>
