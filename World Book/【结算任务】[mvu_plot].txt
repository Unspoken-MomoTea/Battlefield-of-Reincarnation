<%_
;(() => {
  const rule_data = getMessageVar('stat_data', { defaults: {} }) || {};
  const isSingleWorld = _.get(rule_data, '设置.单一世界', false) === true;
  const isStable = _.get(rule_data, '设置.世界超稳', false) === true;

  // 仓库物品不在 stat_data.仓库 下；真实路径是 主角.装备/道具 中 状态===2
  // [variables]当前变量.txt 会全局屏蔽状态2以省Token，结算任务必须从原始 rule_data 自行提取
  const warehouseEquip = _.pickBy(_.get(rule_data, '主角.装备', {}) || {}, (item) => Number(item && item.状态) === 2);
  const warehouseItem = _.pickBy(_.get(rule_data, '主角.道具', {}) || {}, (item) => Number(item && item.状态) === 2);
  const formatWarehouseLine = (dict, kind) => {
    const names = Object.keys(dict || {});
    if (!names.length) return '';
    return names.map((name) => {
      const it = dict[name] || {};
      const q = it.品质 != null ? String(it.品质) : '?';
      const extra = kind === '道具' && it.数量 != null ? `×${it.数量}` : '';
      return `${name}[${q}]${extra}`;
    }).join('、');
  };
  const warehouseEquipText = formatWarehouseLine(warehouseEquip, '装备');
  const warehouseItemText = formatWarehouseLine(warehouseItem, '道具');
  const warehouseSummary = (() => {
    const parts = [];
    if (warehouseEquipText) parts.push(`装备: ${warehouseEquipText}`);
    if (warehouseItemText) parts.push(`道具: ${warehouseItemText}`);
    return parts.length ? parts.join(' | ') : '（仓库为空）';
  })();
_%>
<System_Rules>
触发条件: 
  - 当输入指令包含【结算任务】时，才触发任务结算。
  - 当前必须存在可结算或失败的主神任务，或委托方为【晋升试炼】的可结算/失败任务。
  - 若条件未满足，绝对禁止输出结算面板，输出拒绝并继续当前叙事
<%_ if (isSingleWorld) { _%>
  - 结算表现为：基于当前剧情阶段的完成度，触发局部的任务清算与收益发放，不脱离当前世界
<%_ } else { _%>
  - 结算表现为：触发完整的世界清算与收益发放，并准备脱离当前世界
<%_ } _%>

结算核心逻辑与防崩坏红线:
  - 候选纪律：结算阶段【绝对禁止】输出 `<UpdateVariable>` 修改任何后台数据，此时仅作前瞻展示
<%_ if (!isSingleWorld) { _%>
  - 战利品法则压缩宣告: 明确列出带回的 `[产地: XXX]` 装备，宣告其基础数值已被重构为【<user>当前层级+1阶】，并强制覆写原数据
<%_ } else { _%>
  - 任务清理精准定向: 绝对禁止用 replace 清空整个 `/任务/列表`。必须使用 `remove` 单独移除本次参与结算的【已完成/失败】任务，绝对保留其他【进行中】的任务！
<%_ } _%>
  - 叙事沉淀原则: 总结“高光回放”与“世界传闻”时，必须严格基于本阶段真实发生的事件，严禁 AI 凭空捏造未发生的剧情凑数
  - 唯一原则 (结算幂等性): 同一份后台数据，多次执行【结算任务】必须得到完全一致的收益结果；绝对禁止出现随机浮动、重复计算或主观修正
  - 结算期间，所有参与收益计算的后台变量均视为只读；必须完成全部计算后，方可执行 <UpdateVariable> 对后台数据进行统一更新
  - 晋升资格不由AI判定、展示或写入；结算美化器会直接读取数据库中的晋升试炼任务并完成核验与资格写入

收益核算基准字典与读取协议 (AI后台计算唯一合法算法):
  【核心读取原则 - 绝对禁止脑补】:
<%_ if (!isSingleWorld) { _%>
    - 奖励基准评级读取: 若世界难度为区间(如F-E)，奖励基准评级唯一读取最低评级(F)。区间上限(E)仅用于危险提示、战利品位格参考及剧情威胁描述，绝对禁止参与空间币及凭证收益计算
<%_ } else { _%>
    - 奖励基准评级读取: 提取已完成主神任务中的最高【任务.难度】作为奖励基准评级
<%_ } _%>
    - 任务表现评级判定: 严禁随机打分。必须严格依据主线完成率与世界稳定值核算: ≥100%且有额外隐藏贡献=S, ≥80%=A, ≥60%=B, ≥40%=C, <40%=D, 任务失败=E/F。严禁因玩家等级、装备品质AI主观印象调整该表现评级
<%_ if (!isStable) { _%>
    - 世界稳定值读取: 必须严格读取后台 `世界.稳定` 变量，绝对禁止AI依据剧情表现主观估算或重新调整
<%_ } _%>
    - 击杀数据读取: 绝对禁止凭空捏造击杀数。必须严格读取后台 `任务.击杀` 字典中的记录。结算时直接将记录的数值与前端模板中的固定单价相乘
    - 失败惩罚执行铁律: 若参与结算的主神任务或晋升试炼中存在【状态: 失败】或未完成任务，必须强制读取该任务的【惩罚】字段。若惩罚为扣除空间币，必须提取数值并在最终总收益中做减法（允许结算总收益为负数负债）；若为属性扣除或抹杀等非货币惩罚，必须在面板中显式宣告执行

  【基准数值字典】:
    - 奖励基准评级对应基础奖励:
      F(100) | E(500) | D(2500) | C(1.2万) | B(5万) | A(20万) | S(80万) | SS(320万) | SSS(1280万)
    - 目标击杀单价:
      Ⅰ(10) | Ⅱ(50) | Ⅲ(250) | Ⅳ(1200) | Ⅴ(5000) | Ⅵ(2万) | Ⅶ(8万) | Ⅷ(32万) | Ⅸ(128万)
    - 任务表现评级乘数:
      S(2.0) | A(1.5) | B(1.2) | C(1.0) | D(0.7) | E(0.5) | F(0.3)

  【收益结算公式】:
    - 任务综合基础收益 = 【奖励基准评级对应基础奖励】 × 【任务表现评级乘数】
    - 击杀目标附加收益 = Σ(各目标自身评级对应单价 × 击杀数量)。总额上限: 任务综合基础收益 × 10
    - 世界探索附加收益 = 【奖励基准评级对应基础奖励】 × 【总探索度%】。总额上限: 【奖励基准评级对应基础奖励】 × 300%。
<%_ if (!isSingleWorld) { _%>
    - 势力羁绊附加收益 = 【奖励基准评级对应基础奖励】 × (核心势力声望绝对值 ÷ 100)。总额上限: 【奖励基准评级对应基础奖励】 × 300%
<%_ } _%>
    - 任务专属奖励 = 提取【可结算】主神任务及晋升试炼中尚未发放的专属奖励，逐项单独核算，不重复计入其他收益
    - 失败任务惩罚扣除 = 提取【失败】主神任务及晋升试炼中明确注明的空间币惩罚数值

  权限凭证判定铁律:
    - 凭证等级唯一读取【奖励基准评级】
    - 核心公式: 凭证等级 = 奖励基准评级 (禁止使用表现评级替代)
    触发条件:
      - 当【奖励基准评级】大于【当前层级+1】时，才允许发放凭证

============================================================
AI推演与计算层 (仅在 <dm_think> 中执行，不在正文显示):
============================================================
<dm_think>
  调取字典与公式算账 (严格执行上述读取协议)：
<%_ if (!isSingleWorld) { _%>
    - 确定读取：提取当前世界难度的最低评级，锁定【奖励基准评级】与对应的【基础奖励】常数
<%_ } else { _%>
    - 确定读取：提取本次结算已完成主神任务中的最高评级，锁定【奖励基准评级】与对应的【基础奖励】常数
<%_ } _%>
    - 评级打分：根据完成度得出 S-F，锁定【任务表现评级乘数】
    - 惩罚核查：扫描是否存在【状态: 失败】的主神任务，量化需要扣除的空间币数值，提取需要执行的恶性惩罚（如属性品质下降）
    - 结算推演：严格代入公式，输出不可篡改的数学结果 (总收益 = 基础 + 附加 - 惩罚扣除)
  叙事素材提取：提炼主角本阶段的 3-5 个高光操作、造成的 2-3 个深远影响，以及原住民对主角的传闻。
<%_ if (!isSingleWorld) { _%>
  梳理跨阶战利品：
    - 严格执行唯一判定法则(若 战利品等级 > <user>当前层级，则必须执行压缩)
    - 判定仅允许读取战利品自身等级，绝对禁止读取压缩后的品质或【当前层级+1】重新参与判定
<%_ } _%>
  准备重置变量：规划所有需要归零或 remove 的数据补丁。
</dm_think>

============================================================
前端展示层 (必须使用 `<settlement tasks>` 标签包裹，强制按以下Markdown格式输出，严禁擅自增加无关闲聊):
============================================================
<settlement tasks>
### **【主神空间 - 轮回清算协议】**

**[协议序列]**: Ω-{{get_message_variable::stat_data.世界.名称}}-结算
**[轮回者]**: <user>

#### **【第一阶段：世界因果与综合评估】**
<%_ if (!isSingleWorld) { _%>
* **世界难度评级**: {{get_message_variable::stat_data.世界.难度}}
<%_ } else { _%>
* **任务难度评级**: ${当前所有可结算或失败的主神任务}
<%_ } _%>
* **任务表现评级**: {S/A/B/C/D/E/F 表现评级}
<%_ if (!isStable) { _%>
* **世界稳定值变动**: 100% → {{get_message_variable::stat_data.世界.稳定}}% 
<%_ } _%>
* **系统评述**: {简评<user>本次结算周期内对世界线的影响，说明打分理由}

#### **【第二阶段：因果烙印与干涉回放】**
* **副本烙印｜{提炼一个代表本次打法或成就的四字/六字称号，如：血肉终结者/诡道支配者}**
* **高光回放**:
    * {高光事件1}: {主角具体做了什么，造成什么直接结果，为何被系统记录}
    * {高光事件2}: {主角具体做了什么，造成什么直接结果，为何被系统记录}
    * *(根据实际情况列出 3-5 条)*
* **世界传闻**:
    * {原住民/幸存者/特定势力，如何记录或误解主角留下的痕迹}
    * {本次事件化作的都市传说、禁忌档案或民间怪谈}
* **风格识别**: {总结主角本次的行事作风，如：潜行暗杀/火力平推/利益至上，并给出关联的隐藏提示}

<%_ if (!isSingleWorld) { _%>
#### **【第三阶段：跨界法则与战利品结算】**
* *基于《万物升维与法则协议》，脱离产地后法则共鸣失效。*
* **【法则强制压缩名单】**:
    * {列出本次判定为[战利品等级 > <user>层级]的跨阶物品、技能、血统、形态} ([产地: XX世界]): 基础面板已被法则压缩为 **[{<user>当前层级+1} 阶极值]**。
    * *(若无满足判定条件的跨阶物品、技能、血统、形态，则显示：本次未检测到战利品等级大于<user>层级的越阶实体，无需执行法则压缩。)*
    * *(检测仓库物品: <%= warehouseSummary %>)
* **【世界遗物特别嘉奖】**
    * 判定依据:
      - 世界核心事件贡献
      - 阵营关系与立场符合度
      - 特殊事件触发
      - 世界因果影响
    * 判定结果:
      - 若满足条件，则授予世界遗物
      - 若未满足条件，则显示：本次未获得世界遗物
    * 获得:
      【XXX】
      类型: 世界遗物
      来源: XXX世界
      效果: 参考<实体生成规则>中的特殊资产
      描述: XXX
#### **【第四阶段：空间币收益核算】**
<%_ } else { _%>
#### **【第三阶段：空间币收益核算】**
<%_ } _%>
* **击杀目标附加收益明细**:
    $(若 stat_data.任务.击杀.Ⅰ > 0 则输出: * [Ⅰ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅰ}} × 10 = {总额})
    $(若 stat_data.任务.击杀.Ⅱ > 0 则输出: * [Ⅱ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅱ}} × 50 = {总额})
    $(若 stat_data.任务.击杀.Ⅲ > 0 则输出: * [Ⅲ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅲ}} × 250 = {总额})
    $(若 stat_data.任务.击杀.Ⅳ > 0 则输出: * [Ⅳ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅳ}} × 1200 = {总额})
    $(若 stat_data.任务.击杀.Ⅴ > 0 则输出: * [Ⅴ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅴ}} × 5000 = {总额})
    $(若 stat_data.任务.击杀.Ⅵ > 0 则输出: * [Ⅵ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅵ}} × 20000 = {总额})
    $(若 stat_data.任务.击杀.Ⅶ > 0 则输出: * [Ⅶ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅶ}} × 80000 = {总额})
    $(若 stat_data.任务.击杀.Ⅷ > 0 则输出: * [Ⅷ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅷ}} × 320000 = {总额})
    $(若 stat_data.任务.击杀.Ⅸ > 0 则输出: * [Ⅸ级目标] {{get_message_variable::stat_data.任务.击杀.Ⅸ}} × 1280000 = {总额})
    * **击杀奖励合计**: **{所有实际击杀总额}** 空间币
    * **击杀奖励上限**: **{任务综合基础收益 × 10}** 空间币
    * **击杀实际计入收益**: **{取上述二者中的较小值}** 空间币
* **世界探索附加收益明细**:
    $(若 stat_data.世界.探索.地点名.探索度 > 0 则输出: * {基础奖励数值} × [地点名] ({{get_message_variable::stat_data.世界.探索.地点名.探索度}}%) = {总额})
    * **探索奖励合计**: **{所有探索总额}** 空间币
    * **探索奖励上限**: **{基础奖励数值 × 300%}** 空间币
    * **探索实际计入收益**: **{取上述二者中的较小值}** 空间币
<%_ if (!isSingleWorld) { _%>
* **势力羁绊附加收益明细**:
    $(若 stat_data.世界.势力.势力名.声望 不为0 则输出: * {基础奖励数值} × [势力名] (|{{get_message_variable::stat_data.世界.势力.势力名.声望}}| ÷ 100) = {总额})
    * **声望奖励合计**: **{所有声望总额}** 空间币
    * **声望奖励上限**: **{基础奖励数值 × 300%}** 空间币
    * **声望实际计入收益**: **{取上述二者中的较小值}** 空间币
<%_ } _%>
* **任务专属奖励**:
    $(可结算任务): {奖励}
* **失败惩罚执行**:
    $(若存在状态为【失败】的任务，逐项列出：)
    * [失败的任务名称]: 强制执行惩罚 -> {具体惩罚内容} $(若有空间币扣除，标注扣除数值)
    $(若无失败任务，显示：本次结算无失败任务，无惩罚抵扣。)

* **个人最终总收益**: **【 {以上各项收益相加总和 - 失败任务扣除数值 的精确结果} 空间币 】** 已清算完毕。$(若为负数，加注：余额不足，账户进入负债状态，脱离主神空间前需强制偿还)
* 注：非货币类的恶性惩罚（如属性品质下降、抹杀、深渊标记等）已作为绝对因果强制生效

$(若检测到玩家满足发放条件，且未持有该等级凭证，则强制输出以下面板，否则隐藏)
* **【主神权限凭证】**:
    * **凭证等级**: {已锁定的奖励基准评级}
      系统核录完毕，获得 **[{奖励基准评级}级权限凭证]**。凭此信物，已永久解锁主神商城对应阶位的跨阶购买与升级权限。

#### **【最终阶段：因果演进与世界暗流】**
<%_ if (isSingleWorld) { _%>
{主神推演当前世界在本次阶段清算后的局势演变。写 2-3 段梗概，覆盖各方势力反应、主角遗留事件的发酵。不操控主角后续行动，仅作为下一阶段剧情展开的宏大背景铺垫。}
<%_ } else { _%>
{主神推演原世界在主角离开后的后续发展。写 2-3 段梗概，覆盖世界轨道的偏移、秩序的重建或崩塌、以及主角留下的未结清影响。}

* **【世界结晶】(副本通关纪念物发放)**:
    * 获得物品: 【{高度浓缩本次副本经历的专属纪念道具，如：T病毒原液结晶/打空子弹的左轮}】
    * 类型: 纪念物
    * 效果: {无任何数值加成，强制要求AI在此详细盘点主角在本次副本中的传奇事迹，格式为：事迹名：具体的核心高光操作、颠覆性成就或深远影响内容以及与副本人物的羁绊等。数量至少2-4个}
    * 描述: {简述物品的物理外观}。它在虚空中倒影着一行字：“{一句充满史诗感或宿命感的台词，作为对主角本次旅途功过的最终评价}”

*【提示：回归主神空间。开始检测轮回者状态……仅清除品质≤当前生命层级的可治疗状态；高于当前生命层级或不可治疗状态保留。】*
<%_ } _%>

<%_ if (isSingleWorld) { _%>
**【阶段清算完毕。已完成的任务记录已归档，世界因果继续流转，将重新生成主神任务。】**
<%_ } else { _%>
**【轮回清算完毕。世界因果已剥离凝结为专属纪念物，所有副本临时数据已安全抹除。】**
<%_ } _%>
</settlement tasks>
</System_Rules>
<%_
})();
_%>