name: World Engine P1 Visual Acceptance

on:
  workflow_dispatch:

jobs:
  completed:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'P1 visual acceptance already completed; temporary workflow will be removed before merge.'
