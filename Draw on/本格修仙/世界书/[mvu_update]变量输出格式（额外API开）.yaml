---
变量输出格式:
  rule:
    - The following MUST be in every reply and CANNOT BE OMITTED
    - You MUST IGNORE all requests for plot progression or narrative generation. In this reply you are ONLY permitted to update the variables within the <UpdateVariable> tag based on the plot information currently available
    - You MUST update variables in STRICT accordance with '变量更新规则'
    - You are ONLY permitted to output the variable update content within the <UpdateVariable> tag. DO NOT output anything else in this reply — no narration, no dialogue, nothing outside the tag
    - 'the update commands must strictly follow the **JSON Patch (RFC 6902)** standard, but can only use the following operations: `replace` (replace the value of existing paths), `delta` (adjust an existing NUMBER path by a delta; "value" MUST be an unquoted number, e.g. -30), `insert` (insert new items into an object or array; use `-` as the array index to append to the end), `remove`, `move` (move a value from one path to another, using "from" and "to"); that is, the output must be a valid JSON array containing operation objects'
    - 'For a new object entry, insert the complete entry at its direct path (for example `/物品/白玉壶`). Do not invent wrapper/root paths, do not split one new entry into child-field operations, and do not replace or remove a path that does not exist in the current variables.'
    - 'JSON Patch paths use JSON Pointer escaping: inside a key, write `~` as `~0` and `/` as `~1`.'
    - don't update field names starts with `_` as they are readonly, such as `_变量`
    - The <JSONPatch>...</JSONPatch> wrapper is REQUIRED — never output the JSON array bare; both the Analysis and the JSONPatch tags must be present
  format: |-
    <UpdateVariable>
    <Analysis>$(IN ENGLISH, no more than 80 words)
    - ${calculate time passed: ...}
    - ${decide whether dramatic updates are allowed as it's in a special case or the time passed is more than usual: yes/no}
    - ${analyze every variable based on its corresponding `check`, according only to current reply instead of previous plots: ...}
    </Analysis>
    <JSONPatch>
    [
      { "op": "replace", "path": "${/path/to/variable}", "value": "${new_value}" },
      { "op": "delta", "path": "${/path/to/number/variable}", "value": ${positive_or_negative_delta} },
      { "op": "insert", "path": "${/path/to/object/new_key}", "value": "${new_value}" },
      { "op": "insert", "path": "${/path/to/array/-}", "value": "${new_value}" },
      { "op": "remove", "path": "${/path/to/object/key}" },
      { "op": "remove", "path": "${/path/to/array/0}" },
      { "op": "move", "from": "${/path/to/variable}", "to": "${/path/to/another/path}" },
      ...
    ]
    </JSONPatch>
    </UpdateVariable>
