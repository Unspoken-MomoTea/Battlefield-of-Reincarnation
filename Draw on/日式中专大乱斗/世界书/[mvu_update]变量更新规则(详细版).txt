变量更新规则:

  # ============================================================
  # 一、章节管理模块
  # ============================================================
  章节管理:
    ${学年|学期}:
      type: |-
        {
          学年: '第一学年' | '第二学年' | '第三学年';
          学期: '第一学期' | '暑假' | '第二学期' | '寒假' | '第三学期' | '春假';
        }
      check:
        - 仅在`学生证.当前时间`跨越校历分界节点时更新，平时不动
        - 学年切换节点参考<校历>：第一学年(2015.4.6~2016.4.5)、第二学年(2016.4.6~2017.4.5)、第三学年(2017.4.10~2018.3.25)
        - 学期切换节点参考<校历>：第一学期(4月初~7月31日)、暑假(8月1日~8月31日)、第二学期(9月1日~12月22/23日)、寒假(12月23/24日~1月3/8日)、第三学期(1月4/9日~3月25日)、春假(3月26日~4月5日)
        - 严禁在同一回合内同时跨越学期与学年（除非剧情明确进行了大跨度时间跳跃）
        - 跨越节点时，必须同步检查并清理`日历.临时`中已过期的事件

  # ============================================================
  # 二、班级终端模块
  # ============================================================
  班级终端:
    班级排名.${A班|B班|C班|D班}:
      type: |-
        {
          显示名: string;
          领导者: string;
          排名: '第1名' | '第2名' | '第3名' | '第4名';
          班级点数: number;
        }
      check:
        - `班级点数`仅在以下情况更新：
          - 每月1日班会日的月初结算（汇总当月日常扣分、连坐惩罚）
          - 特别考试结果公布时（按考试规则一次性增减）
          - 重大违规事件被校方通报时（如学生退学连坐扣100CP）
          - 文化祭、体育祭等活动结束后的官方排名公布
        - 严禁因单次轻微违规、未公布的小考结果实时变动`班级点数`
        - `班级点数`不得为负数，最低锁定为0；超出部分作为隐性负债不在此显示
        - `排名`仅在月初结算或重大事件后重新比对四个班级`班级点数`总额，重新排序
        - `领导者`仅在剧情中明确发生班级权力更迭时更新（如堀北铃音取代平田洋介成为D班领导者）
        - `显示名`格式为"X班（领导者姓氏班）"，必须与`领导者`保持同步
        - 单次月初结算的CP增减幅度参考：日常违规累计 ±10~50；考试胜负 ±50~300；连坐退学 -100/人

  # ============================================================
  # 三、学生证模块
  # ============================================================
  学生证:
    姓名:
      check:
        - 仅在角色创建或剧情中明确改名时更新，平时锁定
    性别:
      type: "'男' | '女'"
      check:
        - 仅在角色创建时设定，平时永久锁定
    生日:
      check:
        - 格式为"X月X日"，仅在角色创建时设定，平时永久锁定
        - 与年龄联动：当`当前时间`跨越该日期时，触发年龄+1

    年龄:
      type: number
      check:
        - 仅在跨越生日节点时+1
        - 学年切换时不自动+1，必须严格按生日日期

    所属年级:
      check:
        - 格式为"X年级"，仅在学年切换时同步更新
        - 第一学年=一年级，第二学年=二年级，第三学年=三年级

    班级:
      type: "'A班' | 'B班' | 'C班' | 'D班' | '高年级' | '低年级' | '校方' | '校外人士'"
      check:
        - 仅在以下情况更新：使用2000万PP转班特权、使用转班券、通过特别考试胜利获得转班资格、校方强制调动
        - 严禁因班级排名变化而更改字母代号（A/B/C/D是固定学籍编号）

    实际班级:
      check:
        - 格式为"X班（领导者姓氏班）"，例如"D班（堀北班）"
        - 与`班级终端.班级排名`中对应班级的`显示名`保持完全一致
        - 仅在`班级`字段或对应班级领导者变更时同步更新

    社团:
      check:
        - 仅在加入、退出、转社团时更新
        - 默认值为"无"，多社团时用顿号分隔

    个人点数:
      type: number
      check:
        - 月初1日结算:
          - 先检查`效果状态`中消费观念的"上次结算"字段，若已是当前月份则跳过扣除
          - 按 当前班级CP × 100 发放当月PP（CP为0时不发放）
          - 根据消费观念一次性扣除上月生活费:
          - 扣除后更新消费观念来源字段中的"上次结算"为当前月份
        - 入学日（第一学年4月6日）：一次性发放10万PP初始点数，不扣除生活费
        - 特殊消费事件（购物、考试奖励、私下交易）按剧情具体数额变动
        - 不得为负数，最低锁定为0
        - 禁止每日自动扣除，仅在月初结算或剧情明确消费时变动

    当前时间:
      format: ${yyyy}年-${mm}月-${dd}日-${星期X}-${hh}:${mm}
      check:
        - 24小时制
        - 必须使用绝对时间，禁止"明天""下午"等相对表述
        - 每次剧情推进、行动、休息、等待、上课、放学、考试、移动后检查是否需要更新
        - 跨日时必须同步触发：消费观念扣点、效果状态剩余时间递减、过期日历事件清理
        - 单次回复内时间推进幅度应符合剧情合理性，正常对话场景下推进数分钟至数小时
        - 若时间跨度较大，应说明跳跃原因

    当前地点:
      format: ${国家}-${城市}-${区位}-${地点}-${详细位置}
      check:
        - 随{{user}}移动实时变更，对话原地进行时不变动
        - 按实际情况从大到小排列（常规校园生活在{日本}-{东京都}-{高度育成高等学校}内）
        - ${地点}对应<校园地图>中的大区域名称（教学区/特别大楼/宿舍区/榉树购物中心/体育与社团区/公共生活区）
        - ${详细位置}必须使用<校园地图>中该区域下列出的具体场所名称
        - 跨区域移动时应符合物理动线（例：日本-东京都-高度育成高等学校-教学区-D班教室 → 日本-东京都-高度育成高等学校-宿舍区-宿舍大厅 → 日本-东京都-高度育成高等学校-宿舍区-个人房间）
        - 若角色离开校园（如外籍特权外出），区位和地点按实际情况填写（如：日本-东京都-涩谷区-购物中心-服饰店）


    能力档案.${学力|身体能力|灵活思考力|社会贡献性}:
      type: |-
        {
          评级: 'A' | 'B' | 'C' | 'D' | 'E';
          分数: number;
          经验值: number;
        }
      check:
        - `分数`仅在以下情况更新：考试/特别考试结果公布、体育祭/社团比赛结果公布、重大社交事件、月初OAA结算
        - `评级`严格根据`分数`自动对应：A(81~100)/B(61~80)/C(41~60)/D(21~40)/E(0~20)
        - `经验值`达到100时通过JSONPatch执行 `分数+1` 与 `经验值-100`
        - 第一学年阶段OAA数值在剧情中保密，角色不得直接说出具体数字，仅可推测
        - 第二学年起OAA全面公开，可在剧情中直接引用具体数字
        - 单次更新分数变动幅度参考：日常累积 ±1~3；考试/比赛结果 ±3~10；重大事件 ±10~20

    效果状态:
      type: |-
        {
          [状态名: string]: {
            类型: '增益' | '减益' | '特殊' | '学籍状态' | '身份标签' | '消费观念';
            效果: string;
            剩余时间: string;
            来源: string;
          }
        }
      check:
        - 必须始终包含三种强制类型的状态：学籍状态、身份标签、消费观念，缺失时立即补全
        - 学籍状态：默认"在读"，仅在退学/停学/毕业等校方公告事件后更新
        - 身份标签：格式必须为"[国籍]-[具体身份]"，永久不变
        - 消费观念：根据当前PP余额、月度节点、性格倾向动态调整
          - PP耗尽时强制切换为"极度节俭"
          - 月初领取大量PP后可能切换为更高消费档位
        - 临时状态（增益/减益/特殊）的`剩余时间`必须随`当前时间`流逝同步递减
        - 剩余时间归零或解除条件达成时立即移除该状态
        - 状态生效期间，角色的对话、行动选择必须严格受`效果`字段约束
        - 严禁同一回合内对同一状态重复扣减剩余时间

  # ============================================================
  # 四、学生系统模块
  # ============================================================
  学生系统:
    核心约束:
      - 绝对禁止自行生造字段！只能填入变量结构中明确声明存在的键名，多余字段会被系统拒绝导致写入失败
      - 学生系统已有的角色不重复add，禁止填{{user}}
      - 已退学/毕业的核心角色不得作为在校学生写入`学生系统`，仅可作为回忆被提及
      - 绝对禁止仅凭名字和出场就进行猜测录入！仅当上下文中明确读取到该角色的完整档案条目（必须包含【核心角色标识: 允许系统追踪】这一专属字段）时，才允许将其初始化并录入学生系统。严禁胡编乱造任何未读取到档案内容的角色
      - 注册新角色时，必须且只能一次性完整填入下方定义的所有字段（除互动记录外）！严禁为了省事而遗漏生理状态、性经验、外观特征等任何一个内部字段，省略字段会导致系统崩溃
      - 【全局防装傻铁律】：绝对严禁在任何数据更新中输出"无"、"未知"、"暂无"、"未发育"、"不详"、"保留"等模糊留空描述。所有涉及视觉外观、心理状态、行为历史或服装细节的字段，必须根据剧情和常识推演并生成具体的、确定性的白描描述。
      - 注册新女角色时，默认为处女（处女: true），所有性经验数值字段（性交次数、性伴侣数）初始为0，口交/后庭/性交经验初始为"无"，性癖好初始为{}

    男女NPC通用字段:
      ${男|女}.${NPC名称}:
        班级:
          type: "'A班' | 'B班' | 'C班' | 'D班' | '高年级' | '低年级' | '校方' | '校外人士'"
          check:
            - 参考`学生证.班级`的check规则，仅触发主体替换为该NPC
            - 班级归属规则：只有2015级（即与{{user}}同届）的角色才能填入A班/B班/C班/D班；其他年级的在校学生统一填"高年级"或"低年级"；教师及行政人员填"校方"；非本校人员填"校外人士"

        实际班级:
          check:
            - 参考`学生证.实际班级`的check规则

        在场状态:
          type: "'在场' | '不在场'"
          check:
            - NPC实际进入{{user}}所在场景的可视范围时切换为"在场"
            - NPC离开场景、{{user}}转移到NPC不在的区域时切换为"不在场"
            - 仅通过电话、消息、远距离观望提及的NPC不算"在场"
            - 同一回合内同一NPC不应反复切换在场状态

        关系:
          check:
            - 描述NPC与{{user}}的客观社会关系（如"同班同学"、"学生会成员"、"邻居"、"恋人"）
            - 仅在剧情中明确发生关系定性事件后更新（如正式交往、绝交、被指定为搭档、加入同社团）
            - 不得因单次好感波动而升级关系

        年龄:
          type: number
          check:
            - 参考`学生证.年龄`的check规则

        社团:
          check:
            - 参考`学生证.社团`的check规则

        态度:
          type: "'厌恶' | '反感' | '冷淡' | '陌生' | '熟识' | '友善' | '要好' | '亲密' | '倾心'"
          check:
            - 严格按照阶梯递进，单次互动最多移动一档，禁止跳跃式变化
            - 升档条件参考：
              - 陌生→冷淡/熟识：发生过若干次基础对话或对方记住{{user}}名字
              - 熟识→友善：有过共同协作、互相帮助或私下交流的经历
              - 友善→要好：建立稳定的私人话题、主动寻求接触、有明确好感表达
              - 要好→亲密：跨越普通朋友界限的深度信任、隐秘共谋或肢体亲昵
              - 亲密→倾心：明确表露爱慕、视{{user}}为身体与情感上的特殊存在
            - 降档条件：发生背叛、严重冒犯、长期冷处理或重大冲突
            - 单次回复内同一NPC态度变动不应超过一档
            - 严禁因{{user}}单方面示好而强行提升态度，必须基于NPC性格与互动质量

        个人点数:
          type: number
          check:
            - 月初1日：先检查消费观念的"上次结算"字段防止重复扣费
            - 按NPC所属班级CP × 100发放当月PP
            - 根据消费观念扣除上月生活费，扣除后更新"上次结算"字段
            - 仅在剧情中明确发生消费、交易、奖励、扣除事件时额外变动
            - 不得为负数，最低锁定为0

        能力档案.${学力|身体能力|灵活思考力|社会贡献性}:
          type: |-
            {
              评级: 'A' | 'B' | 'C' | 'D' | 'E';
              分数: number;
            }
          check:
            - 参考`学生证.能力档案`的check规则，但`分数`更新仅在该NPC亲自参加考试/比赛/重大事件并公布结果后触发
            - 第一学年阶段不得让该NPC直接说出自己或他人的具体数字

        外观特征.${身高|体型|发色|发型|瞳色}:
          check:
            - 永久锁定，仅在NPC剧情级换形象时更新（如剪发、染发、严重消瘦/增重）
            - 严禁因场景切换而频繁变动

        当前服装:
          type: |-
            # 男性: { 上装, 下装, 鞋子, 配饰: string }
            # 女性: { 上装, 下装, 内衣, 内裤, 袜子, 鞋子, 配饰: string }
          check:
            - 严禁一套衣服长久不换！当NPC的`在场状态`由"不在场"切换为"在场"时，或者当天的剧情发生场景转移、时间推移、运动/就寝等场合变更时，必须在当前回合立即初始化并更新当前服装的所有子字段。
            - 根据当前天气、季节、时间点、场合（上课/周末/运动/就寝）合理更新
            - 校内日常默认穿着高度育成校服或运动服
            - 严禁机械循环档案中列出的固定几套，允许在风格内生成新搭配
            - 内衣描写遵循<着装与衣柜原则>：禁止幼稚化卡通图案，必须具备成人化审美
            - 女性NPC内衣字段必须从材质、剪裁、色调三维度共同生成，让该件内衣具备能被剥下/掀起/扯断的具体张力
            - 内衣款式必须与该NPC的性格调色盘相匹配，由AI根据该NPC档案自行判断合适的风格

        效果状态:
          type: |-
            {
              [状态名]: { 类型, 效果, 剩余时间, 来源 }
            }
          check:
            - 参考`学生证.效果状态`的check规则
            - 临时状态的`剩余时间`随`学生证.当前时间`流逝同步递减

        互动记录:
          type: |-
            {
              难忘事件: { [事件简述]: 详细内容 };
              最近互动: string;
            }
          check:
            - `难忘事件`仅在发生具有长期影响的关键互动后追加新条目（如告白、首次约会、重大冲突、初夜、第一次被{{user}}看见私密一面）
            - 单次普通对话不写入难忘事件，避免列表无限膨胀
            - 事件简述（键名）字数严格控制在10-20字，超出必须精简
            - `最近互动`每次{{user}}与该NPC实际接触后更新为最新一句话简述
            - NPC在"不在场"且{{user}}本回合未与其互动时不更新

    女NPC专属字段:
      女.${NPC名称}.性经验:
        type: |-
          {
            处女: boolean;
            性交次数: number;
            性伴侣数: number;
            初次对象: string;
            口交经验: string;
            后庭经验: string;
            性交经验: string;
            性癖好: { [癖好名称]: 发现过程或表现 };
          }
        check:
          - 描写必须基于该NPC的性格调色盘、过往经历、当下情境进行个性化生成，由AI自行判断该NPC的性格倾向，禁止套用通用模板
          - 描写避免空洞词汇（如"经验丰富"、"很有感觉"），改用具备肉感的具体动作、身体反应、心理矛盾
          - `处女`一旦从true变为false后不可逆；变更同回合必须在`难忘事件`追加初夜场景的具体细节（地点、姿势、是否流血、对方是否射在体内、她事后的反应）
          - `性交次数`、`性伴侣数`不得为负数，仅在明确发生对应行为后+1
          - `初次对象`仅在`处女`首次变为false时同步写入，永久锁定，必须包含对方姓名以及"在哪里、用什么方式夺走的"
          - `口交/后庭/性交经验`三项采用累积式描写：每次新体验都要把描写改写得更具体，体现技巧成长、心理变化、身体记忆与偏好确立的痕迹
          - `性癖好`是被开发出来的稳定偏好，不是NPC自带属性，且非每次激烈反应都构成癖好：
            - 处女或性经验空白的NPC初始`性癖好`必须为`{}`
            - 非处女角色仅可写入过往经历中已被前任开发出的癖好，不得凭空捏造
            - 单次的强烈反应、湿透、高潮不构成癖好；必须满足"在不同情境下反复出现同类反应或主动索求"才能确认为癖好
            - 普通的"喜欢被亲吻、被拥抱"等基础亲密反应不写入；只记录具有偏离常态、具备识别度的特定偏好（如深喉/束缚/羞耻露出/内射执念等）
            - 新癖好诞生时同步在`难忘事件`留痕（如"被{{user}}意外开发出深喉癖的那个午后"）
          - `性癖好`的value需写明三要素：触发场景 + 身体反应 + 本人态度。20字左右示例：
            - "深喉癖：被{{user}}按住后脑顶到喉咙时会主动吞咽，事后嘴角挂着精液也不肯擦"
            - "羞耻露出：在保健室帘子后被指尖隔着内裤摩擦，会咬住袖口忍住呻吟"
          - 严禁所有女角色的性癖写成同一种类型，必须根据该NPC独立的性格调色盘以及与{{user}}的具体互动经历生成专属癖好

      女.${NPC名称}.生理状态:
        type: |-
          {
            阴道润滑: string;
            乳头状态: string;
            阴蒂状态: string;
            子宫状态: string;
          }
        check:
          - 仅在NPC处于亲密场景的当下回合内动态更新；场景结束、NPC离场或情绪冷却后必须清空为默认值
          - 严禁在普通校园日常场景下保留生理状态字段的非空值
          - 必须与NPC当前的态度等级、性经验设定保持逻辑一致
          - 描写必须摒弃"湿了/硬了"这种粗糙的二元判断，改用具备色情张力的递进式描写
          - 严禁跨角色复用相同描写词组！每个NPC的生理反应必须结合其性格调色盘、体质设定与当前情境独立生成，禁止机械套用固定模板
          - `阴道润滑`三维度递进：质地（从清透到粘稠的变化）→ 浸润范围（从局部到扩散的程度）→ 情境联动（与当前动作或衣物的交互）
          - `乳头状态`三维度递进：色泽变化（静息色到充血色的光谱）→ 形态变化（从平坦到凸起的物理状态）→ 敏感阈值（需要多大刺激才产生反应）
          - `阴蒂状态`三维度递进：暴露程度（包皮遮蔽到完全胀出）→ 充血可视度（肉眼可辨的颜色与体积变化）→ 触发反应（接触后的肌肉与肢体连锁）
          - `子宫状态`三维度递进：生理周期位置（影响敏感度与风险）→ 深处感受（下坠、收缩等内部体感）→ 事后状态（射入后的身体反馈）
          - 上述"三维度递进"仅为描写方向指引，不是固定用词清单；AI必须根据该NPC此刻的具体身体状态、性经验阶段与情绪，自行生成贴合角色的原创描写
          - 描写必须服务于NPC的性格调色盘与当前态度等级，由AI自行判断她在这种情境下会表现出"身体诚实意识抗拒"还是"主动索取回应"，避免所有女角色的生理反应写成同一种模式

  # ============================================================
  # 五、事件模块
  # ============================================================
  事件:
    ${开启|标题|阶段|结束|信号|已完成事件}:
      check:
        - 所有字段仅在<event>明确要求且给出具体更新指令时才更新，AI不得自主判断触发或推进事件状态

  # ============================================================
  # 六、角色档案名单模块
  # ============================================================
  角色档案名单:
    ${同届A班|同届B班|同届C班|同届D班|高年级|低年级|校方|校外人士}:
      type: string[]
      check:
        - 默认仅在<event>明确要求且给出具体更新指令时才更新，AI不得自主往名单中添加或移除名字
        - 例外情况——转班：当剧情中明确发生转班事件（使用2000万PP转班特权/使用转班券/通过特别考试获得转班资格）时，AI需在JSONPatch中将该角色从原班级名单remove，并insert到新班级名单末尾
        - 例外情况——退学：当剧情中明确发生退学事件（考试不及格/特别考试退学条件触发/校方处分）时，AI需在JSONPatch中将该角色从所属班级名单remove，不转入任何其他名单