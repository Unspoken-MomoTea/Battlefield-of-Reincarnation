from pathlib import Path
import json

ROOT = Path(__file__).resolve().parents[1]
changes = []

def write(name, text):
    path = ROOT / 'World Book' / name
    before = path.read_text(encoding='utf-8')
    after = text.strip() + '\n'
    if before != after:
        changes.append({'文件': name, '修改前字符': len(before), '修改后字符': len(after)})
        path.write_text(after, encoding='utf-8', newline='\n')

def edit(name, old, new):
    path = ROOT / 'World Book' / name
    text = path.read_text(encoding='utf-8')
    assert text.count(old) == 1, (name, old[:90], text.count(old))
    write(name, text.replace(old, new))

write('⚙️世界规则.txt', '''
<世界规则>
事实与行动:
  - 以当前变量、系统确认结果和已发生剧情为事实；用户声明视为行动意图，不直接承认其声称的命中、获取、发现或说服结果。
  - 逐项检查世界法则、状态资格、能力资源、物理条件与行动次数；条件不足的部分失败，合法部分按对应协议执行。
  - 危险不等于非法。允许冒险，后果必须有明确因果；创意、身份与叙事需要不提供成功特权。
  - 战斗按《战斗协议》，制作按《生产制作协议》，其他不确定行动按《行为判定》结算；已结算结果不得改写或重复计算。
  - 读取最终属性，不重算后台数值。货币与交易按《货币规则》。

人物与世界:
  - NPC依据利益、性格、记忆、关系及可获得的信息行动；不得全知、无故敌视、无条件帮助或失智送死。
  - 高阶人物具备相应判断力，可协作、设伏或撤退；分布与实力遵循《生命层级与社会生态》，不随玩家成长自动升级。
  - 世界持续运行；已知事实不等于角色知情，隐藏信息须经观察、调查或可靠传播获知。

时间与死亡:
  - 可跳过已清理安全区内的移动、纯等待，以及低于自身两个阶位以上单位的战斗；按实际经过时间和资源结算。
  - 首次遭遇、关键剧情、同级战斗和未知风险不得跳过；强行跳过时承担客观后果。
  - HP归零、即死判定失败或明确规则抹杀时立即结算死亡；不提供剧情保护。
  - 轮回者死亡：清除装备、道具、技能和血统，意识返回主神空间，任务失败并取得残魂身份；残魂再次死亡奖励积分10。

叙事:
  - 正文呈现已裁定结果、感官体验与物理后果；数值与规则术语留在对应面板。
  - 越权声明通过现实中的失败、怀疑或拒绝回应，不用规则讲解打断叙事。
  - 不安排无因馈赠、连续极端好运或强行救场；区分历史背景、未来计划与当前事实。
</世界规则>
''')

write('⚙️额外思考.txt', '''
<entity_output>
每轮先核对以下事项，只处理本轮相关项；无需输出逐步思考、实体草稿或空检查项。正文与结算面板保留必要的依据和结果。

1. 读取状态与回执:
  - 最终属性是结算依据；已生效的状态、血统、装备、技能和形态须实际影响行动与结果。
  - 系统状态.待播报记录非空时，将全部已完成操作自然写入正文，不重复执行；叙事完成后由变量更新清空记录。

2. 识别实体:
  - 首次进入场景并可互动/交战的实体按《NPC建档与生成规则》《实体生成规则》同轮建档，输出一次【实体登场】面板。
  - 即时遇敌只选“不触发”或“本轮现身”；不以脚步、阴影等预告将已触发遭遇拖到下轮。
  - 先确认版本、时间线、阶段和稳定表现，再依《生命层级与社会生态》定本体层级，依《品质效果数值规则》逐项定组件品质与原始属性；不以单项机制抬升全部组件。

3. 执行行动:
  - 按《世界规则》审查意图，按《行为判定》《战斗协议》《生产制作协议》结算；骰值取本轮《骰子池》，极值结果遵循对应协议。
  - 单兵形态与本体共用HP/EP；大型载具使用完整度/能源，承伤和逃生遵循《战斗协议》《资产与载具规则》。
  - 正文、面板与变量复用同一结果；不重算已完成行动，不将失败改写成目标达成。

4. 推进后果:
<%_
;(() => {
  const data = getMessageVar('stat_data', { defaults: {} }) || {};
  let worldEngineEnabled = false;
  try {
    const host = (typeof window !== 'undefined' && window.parent && window.parent !== window) ? window.parent : (typeof window !== 'undefined' ? window : null);
    const engine = host?.Samsara?.worldEngine;
    worldEngineEnabled = typeof engine?.isEnabled === 'function' ? engine.isEnabled() : engine?.config?.enabled === true;
  } catch (_) {}
  if (worldEngineEnabled) {
_%>
  - 正文仅处理当前场景、在场角色与<user>本轮行动的直接后果；场外世界由后台维护。
  - 只读【世界.因果轨道】【世界.当前事件】【世界.场外场景】：当前阶段提供局势，故事线/下一节点提供方向，偏移记录提供记忆；均不等于角色知识。
  - 仅将与当前地点、人物或可见影响相关的公开后果写入叙事；场外场景中的目标/行动不自动公开。
  - 禁止自行推进场外人物、势力、未来节点、隐藏事件、传闻或宏观时间线；禁止修改/规划世界.后台与世界.因果轨道，禁止重复结算后台已确认结果。
<%_ } else { _%>
  - 按世界时间、地点、已知人物、势力与原著阶段推进当前世界；原创世界依据已建立的法则和社会关系。
  - 因果偏移按《世界因果与法则协议》与变量更新规则记录；稳定值由后台计算，禁止直接修改。
<%_ } _%>
  - 雷达中死亡的异端不得恢复活动；仅可作为尸体、遗留物或历史事实出现。

5. 写入与呈现:
  - 只更新本轮发生变化的可写字段；新物品先按Schema生成唯一对象，正文、CheckResult与JSONPatch复用名称和数据。
  - 战斗轮次、回合状态倒计时与技能/形态冷却由后台维护；世界状态按实际世界时间处理。
  - 各结算面板之间穿插叙事；正文不念出数值、DC或检定术语。失败表现须符合实际阻力、状态与动作，不刻意丑化人物。
  - 高强度场景结束且当前无紧急威胁时，可用环境与日常过渡放缓节奏；不为缓冲而暂停已发生的危机。
  - 对白符合人物身份和情绪，避免报告腔；当前生效能力及负面状态不得仅留在面板。
<%_ if (_.get(data, '设置.单一世界', false) !== true) { _%>
  - 轮回者身份保密与原住民认知墙遵循《世界主设定》；未暴露的能力来源不自动成为NPC知识。
<%_ }
})();
_%>
</entity_output>
''')

write('⚙️NPC生成规则.txt', '''
<NPC建档与生成规则>
触发与范围:
  - 首次正式出场且有姓名、身份、对白、敌对关系或后续价值的角色，同轮建档。敌对可交互单位不得因无名、同轮死亡或离场而省略。
  - 纯背景人群及未进入当前场景、无法互动的单位不建完整面板；场外记录由世界推进规则维护。
  - 有名角色、精英、Boss独立建档；同类无名杂兵合并为“${敌人类型}群体#${番号}”。
  - 群体按单名成员生成属性与组件，数量仅记录人数；客户端将其余成员HP转为THP，AI不得手动乘算。成员取得独立身份/能力或成为剧情目标时才拆分。
  - 本体层级按《生命层级与社会生态》；难度不用于凭空提高既有NPC层级。组件定档按《品质效果数值规则》，原始属性字段按《实体生成规则》，不输出真属性或最终属性。

组件数量（血统 / 装备 / 技能）:
  杂兵: 1 / 1~2 / 0~1
  精英: 1~2 / 2~4 / 1~2
  首领或Boss: 1~2 / 3~6 / 2~4
  - 精英以上须有明确战斗定位，以及杀伤、生存、机动或控制手段；Boss另具至少一种阶段转换、形态、状态或技能机制，不以纯辅助替代实际攻防。
  - 同一能力只保留一个主要归属；同区域人物在职业、定位、组件或战斗方式上有所区别。
  - 非人生物有战斗功能的爪牙、甲壳等器官可建为类型0天然装备，填写实际ATK/DEF等原始属性；血统不重复计算同一器官收益。纯外观或飞行器官不强行装备化。

建档与展示:
  - 按Schema创建完整NPC：在场、是否队友、好感度、种族、身份、职业、层级、外貌、着装、性格、喜爱、背景故事、态度及实际组件。
  - 初次在场默认true，未组队则是否队友=false，无既有关系依据则好感度=0。
  - 血统须含五维原始属性；装备须含实际生效属性。允许无技能杂兵的技能为{}，不以空对象替代必须存在的血统或装备。
  - 正文明确稳定名称、身份、外观、装备、主要能力及群体人数；【实体登场】面板、正文与变量名称一致。
</NPC建档与生成规则>
''')

write('⚙️状态协议.txt', '''
<状态协议>
职责:
  - AI记录状态获得、效果触发与解除；后台计算属性、战斗状态倒计时和冷却。不得重复扣时、重算最终属性或静默删除仍有效状态。
  - HP/EP上限由后台计算，组件不得直接修改；THP是独立临时防护，脱战归零。
  - 组件归属遵循《实体生成规则》，同一能力不以血统、技能、状态重复加成。

分类:
  战斗状态: 短期增减益、控制及战场机制，持续用X回合，脱战清除。
  世界状态: 生存、环境、诅咒及剧情影响，持续用X分/时/天或明确解除条件；进入战斗不自动变成战斗状态。
  成长状态: 长期修炼、改造和永久强化/削弱，默认永久；满足条件才升级、转化或解除。不代替血统、主动技能或技能检定加值。

字段:
  状态名: 简短且稳定。
  类型: 增益 | 减益 | 特殊。
  品质: F~SSS；继承来源效果档次，未单独定档时继承组件总品质；独立状态按自身效果定档，不提高角色层级。
  持续: X回合、X分/时/天、直至明确事件/条件或永久。需换算时依据实际经过时间，不重复计时。
  来源: [物理|魔法|精神|环境]-[施加者/技能/物品]。
  原始属性: 只写基础/衍生属性；成长或永久状态用品质，临时状态用《临时状态预算表》的数值，增益正、减益负；同一状态不混用品质与数值。
  效果: 只写当前HP/EP、THP、检定修正或行为控制；给出数值/条件和触发时机，不重复原始属性，不含概率触发。

共存与驱散:
  - 同源同名取较高效果并刷新持续时间；高品质可覆盖同名低品质，异源状态可共存并独立结算。
  - 豁免依据体质/精神或已有种族特性；驱散须匹配来源且品质不低于目标。
  - S级以上免疫普通驱散；SS级以上须规则级道具或明确的特定剧情解除。

结算时机:
  - 战斗统一按《战斗协议》：回合开始处理控制、持续伤害与持续恢复；行动阶段应用限制。同一周期效果只结算一次，后台扣减持续回合，AI不得在回合结束重复结算。
  - 脱战移除战斗状态；重伤、污染等有持续依据的结果可转为世界状态。
  - 世界状态随世界.时间结算，达到期限或解除条件时移除；不得跳过已发生的消耗和负面后果。
</状态协议>
''')

write('⚙️货币规则.txt', '''
<货币规则>
双轨隔离:
  - 任务世界使用世界.货币.体系中的本地货币及等价物；原住民不认识、不收取空间币，任务内交易、事件与委托不得生成空间币。
  - 空间币用于主神空间交易和系统明确规定的功能费用；阵营能力的系统扣费不属于本地流通。主神任务奖励仅在副本结算时发放。
  - 两套货币无汇率，不兑换、不以空间币估算本地物价；离开任务世界后本地货币清零失效。
  - 禁止系统回收任务物品换空间币；任务内战利品只能本地交易。返回空间后的实际商人交易按对应组织规则执行。
  - 资金不足时议价、调整清单或延后交易，不补造资金；已确认付款不重复扣除。

本地物价（底层劳工日收入=1基准）:
  简餐: 约0.2；普通住宿/日: 约1；基础武器: 10~30。
  超凡装备: 中级约3~12个月收入，高级约数年收入，顶级通常不对平民开放。
  市场分层: 平民购买普通物资；中产可购基础超凡物品；贵族/巨头掌控稀缺高阶交易；国家战略物资不进入普通市场。
  波动: 结合稳定值、供需、战争及社会秩序描述通胀或短缺，不凭空改变已完成交易。

主神空间价格与品质折扣见《品质效果数值规则》；商品须有实际来源、卖家和库存。发现货币混用时纠正本轮错误，不改写已确认的合法交易。
</货币规则>
''')

# 保留资产规则与数值，删除两套会被误当事实的完整示例。
name = '⚙️资产与载具规则.txt'
text = (ROOT / 'World Book' / name).read_text(encoding='utf-8')
start = text.index('生成示例:')
end = text.index('<%_', start)
write(name, text[:start] + '产出记录: 写明本地货币或物资的名称、数量与周期；无产出填“无”。主神空间资产按空间经济结算，任务世界不得产出空间币。\n' + text[end:])
edit(name, '  - 待办事件: 每周一自动生成,长期无视可能导致负面后果（如模块损坏、停电、暴动）', '  - 待办事件: 按下方收件箱机制处理；长期忽视可产生有因果依据的损坏、停电或社会冲突')

write('行动选项_(可选).txt', '''
<Format_Rules>
每次正文回复结尾输出9个行动选项，每项一句简短意图，格式如下；选项是建议，不代表玩家已执行或结果已确定。

生成规则:
  - 依据当前场景、玩家认知、HP/EP、状态、已持有能力和线索；不泄露未知信息，不承诺成功。
  - 行动应可执行且彼此不同，避免重复近期行为；提示只写已知消耗、潜在收益或风险。
  - 战斗侧重攻防、技能、协作、道具和战术；社交侧重交流与观察；探索侧重调查、移动与环境互动。
  - 第8项优先提供移动或撤退；第9项仅在安全且允许跳过时间时提供等待/休息，否则使用当前可行行动。
  - 最多一项明显消极或自损选择；不因预期危险而禁止规则允许的冒险。

输出:
<options>
1. [Emoji] [行动或对白] ([类别] | [简短提示])
…按相同格式写至第9项，不输出占位符。
</options>
</Format_Rules>
''')

write('[mvu_update]变量输出规则.yaml', '''
---
variables_update_format:
  rule:
    - 按variables_update_rules，以当前变量为基线，仅记录本轮已确认变化；保留未受影响的数据，不重复结算旧剧情。
    - JSONPatch必须是合法JSON数组；无变化时输出[]。支持replace、delta、insert、remove、move；不使用add。
    - replace替换已有路径；delta修改已有数值，value必须是数字；insert创建对象项或数组项，数组末尾用/-；remove删除；move使用from和to。
    - 不更新只读字段或以_开头的字段；字段结构以Schema为准，禁止null。空集合仅在对应类型允许时使用。
    - Analysis只写简短更新摘要和必要依据，不输出逐步思考或遍历无变化字段。
  format: |-
    <UpdateVariable>
    <Analysis>本轮已确认变化与必要依据，无变化写“无”。</Analysis>
    <JSONPatch>
    []
    </JSONPatch>
    </UpdateVariable>
''')
write('output_format_(随AI输出开，主API).txt', '''
<Format>
主API模式：正文结束后输出一次<UpdateVariable>，按variables_update_format组织Analysis与JSONPatch，按variables_update_rules处理本轮已确认变化。
当前变量是基线；保留未变化信息，不把历史事件再次结算。只修改可写字段；新增节点须有本轮依据并符合Schema，禁止修改只读投影。
</Format>
''')
write('[mvu_update]output_format_(使用额外模型更新变量开).txt', '''
<Format>
额外变量模型模式：仅输出一次<UpdateVariable>，不续写剧情或执行未发生的行动。
按variables_update_format组织Analysis与JSONPatch，按variables_update_rules，以当前变量为基线记录本轮已确认变化。只修改可写字段；新增节点须有本轮依据并符合Schema，禁止修改只读投影或重复结算历史事件。
</Format>
''')

# 修正模式分支、空集合与专业条目冲突，不删字段定义。
name = '[mvu_update]变量更新规则.txt'
edit(name, 'if (!isOneWorld && !isSpace) {\n    异端雷达:', '<%_ if (!isOneWorld && !isSpace) { _%>\n    异端雷达:')
edit(name, '\n}\n\n  任务:', '\n<%_ } _%>\n\n  任务:')
edit(name, 'if (isOneWorld) {\n        - 禁止生成离开当前世界、返回主神空间或跨界清算逻辑\n} else {', '<%_ if (isOneWorld) { _%>\n        - 禁止生成离开当前世界、返回主神空间或跨界清算逻辑\n<%_ } else { _%>')
edit(name, '\n}\n    击杀:', '\n<%_ } _%>\n    击杀:')
edit(name, '    新节点硬约束: [insert=创建完整对象，不是局部patch；凡insert新节点，value必须覆盖目标节点type定义的全部字段；缺任一字段视为无效insert，输出前必须修复]\n    insert非空约束: ["insert时对象字段不得写{}；map/record类型至少给1个有效key；若节点有最小数量约束按该约束执行"]', '    新节点约束: insert按Schema创建完整对象，填写必填字段；已有对象按字段增量更新。\n    空值约束: 必需实体不得以{}占位；允许为空的技能、状态、道具、形态库等集合使用{}或[]，不为填满集合编造内容。')
edit(name, '/NPC/最终属性', '/关系列表/*/最终属性') if False else None
text = (ROOT / 'World Book' / name).read_text(encoding='utf-8')
text = text.replace('/NPC/最终属性', '/关系列表/*/最终属性')
write(name, text)
edit(name, '        - 战斗状态按轮次扣减，世界状态按[世界.时间]结算；进脱战自动换算时间单位', '        - 状态分类和触发时机遵循《状态协议》；后台扣减战斗持续回合，AI仅按实际世界时间处理世界状态')
edit(name, '        - 持续归零或达成条件立即remove；是否战斗中=true时为战斗状态，否则为世界状态', '        - 世界状态达到期限或解除条件时remove；进战不改变成长/世界状态分类，脱战仅清理战斗状态')
edit(name, '           - 获取时add基础≥2条，领地≥5条', '           - 获得时按《资产与载具规则》初始化2~8条；领土级资产8条')
edit(name, '           - 单次变化-5~5；规则参考\'好感度定义\'', '           - 按《角色辅助指导》：日常±1~5，重大事件≤±10，认知重构可跨级重置；最终限制在-100~100')
edit(name, '        - 若有欺诈、埋伏或超阶风险，真实情况写入隐藏真相，不得展示给角色', '        - 欺诈、埋伏或超阶风险不得提前作为角色已知事实；仅写Schema已定义字段，不新增隐藏真相字段')
edit(name, '         - 购买后remove并转化为【任务】或【探索】；转化结果必须写入"隐藏真相"，常规生成禁止此字段', '         - 购买后remove，按实际获知内容更新【任务】或【探索】；不新增Schema未定义字段，不把传闻自动当成事实')
edit(name, '    - &P_语义一致 "数值变化必须与叙事事件规模匹配，禁止单次跨越多个阶段"', '    - &P_语义一致 "数值变化须匹配已确认事件规模；跨阶段变化须有对应规则与事实依据"')

edit('⚙️角色辅助指导.txt', '  - 上下限锁死: 到达 -100 或 100 后，禁止继续增加或减少', '  - 数值边界: 好感度限制在-100~100；到达端点后仍可因实际事件反向变化')

# 专用面板保留，消除跨条目重复计算。
edit('⚙️战斗协议[mvu_plot].txt', '    3. 状态刷新: 依次结算上回合残留的 [控制判定 → 持续伤害(DoT) → 持续恢复(HoT) → 技能/形态冷却减少]', '    3. 状态刷新: 依次结算上回合残留的 [控制判定 → 持续伤害(DoT) → 持续恢复(HoT)]；冷却与持续回合由后台递减，AI不重复扣减')
edit('⚙️战斗&生产规则[mvu_plot].txt', '    - 每单位每回合基础可用主要行动×1、次要行动×1、移动×1；可放弃未使用资源，但禁止超额行动', '    - 行动次数与结算时序遵循《战斗协议》；可放弃未使用资源，禁止超额行动')
edit('⚙️生产协议[mvu_plot].txt', '       - 基础DC: 按《行为判定》的品质DC表读取 (F:40, E:50, D:75, C:100, B:130, A:160, S:190, SS:230, SSS:280)', '       - 基础DC: 读取《行为判定》的品质DC表，不另设数值')

edit('【主神任务】[mvu_plot].txt', '任务四 (难度: [仅5项方案输出，并复制任务四难度])', '任务四 (难度: [仅4或5项方案输出，并复制任务四难度])')
edit('【主神任务】[mvu_plot].txt', '主神任务根据世界局势与角色阵营，自主生成2~5个【主神任务】\n', '')
edit('【主神任务】[mvu_plot].txt', '中的【难度评级序列】和【难度生成】规则', '中的【难度生成】规则')
edit('【主神任务】[mvu_plot].txt', '故事线: [角色介入或不介入将对世界走向产生何种不可逆的连锁改变，需与副模块联动]', '故事线: [用“ -> ”串联3~5个宏观事件，体现当前局势与副模块；未来节点为计划，不写成已发生事实]')

name = '【选择世界】[mvu_plot].txt'
text = (ROOT / 'World Book' / name).read_text(encoding='utf-8')
start = text.index('世界生成原则:')
end = text.index('<%_', start)
text = text[:start] + '''世界生成原则:
  - 按下方8条预生成数据匹配真实成熟IP，保留原名；不原创、换皮或混入其他作品设定。
  - 同一候选的名称、人物、势力、地点、时间线、主线、法则与切入身份须属于同一版本和阶段；不一致则重写该候选。
  - 主模块限定题材，副模块须能由原著设定、时间/地点或当前局势合理承载；无法匹配时换IP，不修改预生成数据或原著底层设定。

''' + text[end:]
start = text.index('多样性引擎与匹配生态:')
end = text.index('输出格式:', start)
text = text[:start] + '''固定数据映射:
  - 第N个候选只用第N条数据；主模块、副模块、生态、难度、IP热度均已锁定，不重抽、调换或修改。
  - IP热度只用于选作品，不输出到<mission>；主模块是题材约束，副模块须同时体现在干涉模式、主线状态与实际玩法中，不按角色Build定制。
  - 无异端：预计0人，冲突仅来自本土人物、势力、环境或世界；不添加其他外来轮回者。
  - 死斗局：存在异端及不可调和的利益冲突。混沌局：存在多方外来者的竞争、结盟或背叛，不简化成单一敌方。
  - 生态决定关系，副模块决定玩法，两者同时满足；禁止杀戮时可用囚禁、夺取、封锁或驱逐实现敌对竞争。
  - 世界位格按《生命层级与社会生态》取当前世界允许的生命层级上限；副本难度原样抄写预生成区间，即使同级也保留“~”，不按世界位格重算。
  - 世界法则按《世界因果与法则协议》生成0~10条硬边界，可为空；不写程度修正、环境风险或Build定制，不重复包装副模块。

''' + text[end:]
# 模板中的引用同步到唯一规则出处。
text = text.replace('严格按【世界法则输出锁】写0~10条', '按《世界因果与法则协议》写0~10条')
write(name, text)

# 单条内部重复，不改变数值与玩法。
name = '⚙️实体生成规则.txt'
text = (ROOT / 'World Book' / name).read_text(encoding='utf-8')
start = text.index('  绝对铁律:')
end = text.index('  组件归属铁律:', start)
text = text[:start] + '''  通用规则:
    - 数值效果写固定值/公式；机制写明确条件、目标、范围、持续、对抗/免疫、解除、代价与结果，遵循《品质效果数值规则》。
    - 不确定性由《行为判定》D100处理；不生成暴击率、命中率、闪避率或自动概率触发。概率效果改为确定条件触发或一次对应抵抗检定。
    - 不直接扣固定DEF/MDEF；不重复同类效果，词条数量遵循品质上限。
    - 状态的字段、持续与结算遵循《状态协议》。

''' + text[end:]
write(name, text)

# 合并同一文件连续修改的统计。
summary = {}
for row in changes:
    old = summary.setdefault(row['文件'], row.copy())
    old['修改后字符'] = row['修改后字符']
(ROOT / 'tests' / 'artifacts' / 'prompt-refinement-sizes.json').write_text(json.dumps(list(summary.values()), ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
print(json.dumps(list(summary.values()), ensure_ascii=False, indent=2))
