<compliance>
所有 <ref rule="*"/> 规则必须严格遵循:
  - 所有数值严格参照<rule ref="核心系数总表"/>取值参数,并经面板完整推演,禁止跳过计算直接输出结果
  - 严格读取<status_current_variables>中已装备/使用中的技能/装备/道具,禁止无中生有
  - 所有随机数从<骰子池>按顺序索引取用,禁止自行假设骰值
  - Z%与W为动态求值,仅检索与当前计算属性相关的加成,同类加成取最高一项,不叠加
面板输出规则:
  - 面板连同其规定的外层标签（如<action_info>）一同输出(开闭齐全)
  - 面板插入<gametxt>中相关情节位置,禁止在开头/结尾批量堆叠
  - 所有<!-- ... -->思考内容仅用于定位关键参数取值与逻辑判断,具体计算于面板中执行
  - 使用 {} 包裹的,独占一行,如: {战况总览}
  - 所有含数据的行以 | 开头并以 | 结尾
  - 模板中 [] 为变量,必须填入实际数值或文本；() 为注释,禁止出现在面板输出中
  - 多个同类面板之间必须插入叙事正文,禁止连续堆叠
</compliance>