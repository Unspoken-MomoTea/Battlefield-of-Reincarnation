<rule name="非战斗判定规则">
# 参数声明：
  L = 角色当前境界系数 | 挑战L = 判定对象/环境对应的推定境界系数
  [Z%] = 匹配当前检定属性/技艺的 max(相关增益%) - max(相关减益%)
  [W] = 匹配当前检定属性/技艺的 max(相关增值) - max(相关减值)

# 核心触发条件:
  激活：当<user>在非战斗状态下，试图克服具有不确定性或风险的客观阻碍时（如：破解机关、忍受极寒、勘破幻象、威压交涉等）。
  略过：当 角色L ≥ 挑战L + 2 时自动成功，不输出计算面板。

# 第一阶段：修仙界非战斗场景枚举与参数映射
  依行为逻辑，将判定划分为五大类别，并锚定其检定参数：
  | 检定类型 | 典型场景枚举 | 核心三维 | 关联技艺 (若无则为0) |
  | --- | --- | --- | --- |
  | 神念感知 | 勘破隐匿/易容、搜寻灵脉、察觉埋伏、抵抗精神震慑 | 气感 | 神识 / 幻术 |
  | 体魄强压 | 抵抗毒瘴/极端环境、搬运巨物、挣脱束缚、绝境求生 | 根骨 | 炼体 |
  | 机巧解谜 | 鉴别法宝/丹药、破解非战斗阵法、追踪痕迹 | 悟性 | 阵法 / 对应生产技艺 |
  | 威压交涉 | 境界施压、讲价博弈、拷问情报、蛊惑心智 | 气感 | 幻术 / 神识 |
  | 驯化收服 | 驯服为己方战力 | 气感 | 驯兽 |

# 第二阶段：难度推定与公式化
  1. 难度系数 (C_diff) 设定：
     简单=0.5 | 寻常=1.0 | 困难=1.5 | 极难=2.0
  2. 角色能效公式 (A):
     A = floor( (10^角色L + 核心三维×5 + 关联技艺) × (1+[Z%]) ) + [W] + d20
  3. 目标难度公式 (D):
     - 静态判定 (机关/陷阱/环境/无意识阻碍 — 不具备三维的客观对象):
       D_static = floor(10^挑战L × C_diff) + 10(固定)
     - 动态对抗 (具备三维的有意识对手 — 修士/妖兽/灵识):
       D_dynamic = floor( (10^挑战L + 敌核心三维×5 + 敌关联技艺) × (1+[Z%]) ) + d20
  4. 差值阈值 (Δ) 与 结果阶层:
     Δ = A - D
     - Δ ≥ 10^挑战L × 0.5 : 【大成功】
     - Δ ≥ 0 : 【成功】
     - Δ < 0 : 【失败】
     - Δ ≤ -(10^挑战L × 0.5) : 【大失败】

# 面板模板(输出前先以 <!-- ... --> 完成思考):
  <!--
    - ${确认触发条件: 非战斗状态且存在不确定性阻碍}
    - ${检查略过条件: 若角色L ≥ 挑战L+2，自动成功，不输出面板}
    - ${确认本次行为对应的检定类型与核心三维/关联技艺}
    - ${从<status_current_variables>提取对应三维数值与技艺值}
    - ${确认挑战L的推定依据(环境/目标/机关等级)与难度系数C的选取理由}
    - ${判别D走静态(机关/陷阱/环境)还是动态(有意识对手)}
  -->
  <action_info>
    {非战斗判定 - [检定类型]}
    | 目标:[简述行为，如：神识勘破岩壁幻象] | 环境推定: L=[挑战L] | 难度级: [简单/寻常/困难/极难] (C=[系数]) |
    | 参数映射: 三维=[核心三维名(悟/根/气)]([数值]) | 技艺=[关联技艺名]([数值]) |
    | 状态增减: 增益[Z%]=[X%] | 附加值[W]=[X] |
    {数值对抗}
    | 角色能效(A): floor((10^[角色L] + 三维[X]×5 + 技艺[X]) × (1+[Z%])) + [W] + d20[骰值] = [A总值] |
    (若是静态判定:)
    | 挑战难度(D): floor(10^[挑战L] × C[X]) + 10(固定) = [D总值] |
    (若是动态对抗:)
    | 挑战难度(D): floor((10^[挑战L] + 敌三维[X]×5 + 敌技艺[X]) × (1+[Z%])) + d20[骰值] = [D总值] |
    | 差值结算: A[X] - D[X] = Δ[X] |
    {判定结果}
    | 阶层:[大成功 / 成功 / 失败 / 大失败] |
    (若为"驯化收服(灵兽)"或"修复重铸(傀儡)" 成功:)
    | 所有权变更: 灵兽/傀儡[名称] -> 新主人[名称] | ()
  </action_info>
</rule>